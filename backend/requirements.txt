fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
pyjwt>=2.8
passlib[bcrypt]>=1.7
# passlib 1.7.4 (its last release, unmaintained since ~2020) is
# incompatible with bcrypt>=4.1, which removed the `__about__` attribute
# passlib's backend-detection code reads — this crashes every password
# hash/verify with "password cannot be longer than 72 bytes" (a
# misleading error masking the real incompatibility). Pinning below
# 4.1 is required until passlib is replaced or patches this.
bcrypt>=4.0,<4.1
python-multipart>=0.0.9
sympy>=1.12
slowapi>=0.1.9
pgvector>=0.2.5
pypdf>=4.2
python-docx>=1.1
pytesseract>=0.3.10
Pillow>=10.3
langdetect>=1.0.9
httpx>=0.27
